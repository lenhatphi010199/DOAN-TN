'use strict';

var _AbilityCollection = require('../AbilityCollection');

var _AbilityCollection2 = _interopRequireDefault(_AbilityCollection);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var definition = {
	permissions: [{
		name: 'VIEW_BUTTON',
		roles: ['ADMIN', 'USER']
	}, {
		name: 'EDIT_BUTTON',
		roles: ['USER']
	}]
};

describe('collect', function () {
	test('collect should assign the correct abilities', function () {
		var collection = new _AbilityCollection2.default(definition, ['ADMIN']);
		expect(Object.keys(collection.abilities).length).toBe(1);
	});
});